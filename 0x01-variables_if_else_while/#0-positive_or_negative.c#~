#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* Main -  entry point */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if(n>0){
	  puts("is positive");
	else if(n==0){
	  puts("is zero"_)
	/* your code goes there */
	return (0);
}