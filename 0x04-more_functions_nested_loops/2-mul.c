#include "main.h"

/**
 * mul - multiplies two number
 * @a: number to be multiplied to b
 * @b: number to be multiplied to a
 * Return: the result
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
